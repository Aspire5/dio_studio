import 'package:test/test.dart';

void main() {
  group('dio_studio', () {
    // Tests will be added here as public APIs are implemented.
    // This file verifies that the library can be imported without errors.
    test('library can be imported', () {
      // This test exists to verify the package compiles and the
      // barrel file exports are valid. It will be replaced by
      // real tests once features are implemented.
      expect(true, isTrue);
    });
  });
}
